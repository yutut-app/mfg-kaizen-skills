# A3改善報告書テンプレート

問題解決型QCストーリーの8ステップに対応した1枚報告書。
A3用紙1枚（左半分＝現状分析、右半分＝対策と結果）に収める前提で構成している。

**使い方**：`<>` を実際の内容に置き換える。空欄のまま残さない。書けない項目は「未実施」と明記する。

---

## 表題

| 項目 | 記入欄 |
|---|---|
| テーマ | `<A工程の寸法不良低減>` |
| 対象工程・製品 | `<>` |
| 期間 | `<YYYY/MM/DD 〜 YYYY/MM/DD>` |
| 推進者 | `<>` |
| 型 | `<問題解決型 / 施策実行型 / 課題達成型 / 未然防止型>` |

---

## 1. テーマ選定

**なぜこのテーマを選んだか**

`<QCDのどれに、どれだけ効くか。選定にパレート図を使ったなら、その結果を1文で>`

---

## 2. 現状把握

**事実（推測を混ぜない）**

| 項目 | 値 |
|---|---|
| `<不良率>` | `<0.8%>` |
| `<期間>` | `<2026/04 - 2026/06>` |
| `<件数>` | `<n = 240>` |

**層別の結果**

`<何で層別し、どこに差が出たか。差が出なかったキーも書く>`

**図**：`<パレート図 / ヒストグラム / 管理図>` を添付

---

## 3. 目標設定

| 何を | 現状 | 目標 | 期限 |
|---|---|---|---|
| `<不良率>` | `<0.8%>` | `<0.3%>` | `<2026/09/30>` |

**根拠**：`<なぜこの水準か。基準値・他ラインの実績・顧客要求など>`

---

## 4. 活動計画

| # | 実施事項 | 担当 | 期限 |
|---|---|---|---|
| 1 | `<>` | `<>` | `<>` |
| 2 | `<>` | `<>` | `<>` |

---

## 5. 原因追究

**要因の洗い出し**：`<特性要因図（大骨＝4M） / 連関図>` を添付

**絞り込んだ要因と検証結果**

| 要因 | 検証方法 | 結果 | 真因か |
|---|---|---|---|
| `<>` | `<層別 / 散布図 / 実験>` | `<>` | `<○ / ×>` |

**発生原因**：`<なぜ不良が作られたか>`

**流出原因**：`<なぜ検査で止められなかったか>`

> 流出原因を書けない場合は「未検討」と明記する。省略しない。

---

## 6. 対策立案・実施

| # | 真因 | 対策 | 5Eの区分 | 担当 | 実施日 |
|---|---|---|---|---|---|
| 1 | `<>` | `<>` | `<教育/技術/強化/模範/環境>` | `<>` | `<>` |

**採否の判断**：`<マトリックス図で評価したなら、その軸と結果>`

> 対策が教育（Education）に偏っていないか確認する。仕組みで防ぐ案（技術）を含める。

---

## 7. 効果確認

| 項目 | 対策前 | 対策後 | 目標 | 達成 |
|---|---|---|---|---|
| `<不良率>` | `<0.8%>` | `<>` | `<0.3%>` | `<○ / ×>` |

**比較の条件**：`<対策前と同じ分類・同じ期間長・同じ層別で比較したか>`

**図**：`<パレート図（前後比較） / 管理図>` を添付

**未達の場合**：`<どこまで効いたか。次に何をするか>`

---

## 8. 標準化・定着

| 反映先 | 反映内容 | 改訂日 |
|---|---|---|
| `<QC工程表>` | `<>` | `<>` |
| `<作業標準書>` | `<>` | `<>` |
| `<4M変更管理ルール>` | `<>` | `<>` |

**教育の実施**：`<対象者・実施日・方法>`

**維持の確認方法**：`<SDCAで何を、どの頻度で確認するか>`

> このステップが空欄の報告書は未完成。標準化しなければ必ず再発する。

---

## 9. 残課題・水平展開

| 項目 | 内容 | 担当 |
|---|---|---|
| 残課題 | `<>` | `<>` |
| 水平展開先 | `<他ライン・他機種で同じ対策が有効か>` | `<>` |
